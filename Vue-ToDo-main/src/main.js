import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
// the creatApp function mounts/links the app component onto the dom node with id = app which is the div tag in the
